// Cloudflare Worker config. The site is served as static assets straight from the repo root;
// .assetsignore keeps repo-only files (scripts, docs, config) from being uploaded.
{
  "name": "sleepy-magpie",
  "compatibility_date": "2026-09-01",
  "assets": {
    "directory": "."
  }
}
